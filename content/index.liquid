---

pagination:
  data: collections.articles
  size: 6
  alias: articles
layout: layout.html
title: Hero4Hire
---
<div class="py-8">
  {% comment %}
    <ul>
      {%- for entry in collections.notes %}
        <li>
          <a href="{{ entry.url }}"
            ><code>{{ entry.url }}</code></a
          >
        </li>
      {%- endfor %}
    </ul>
    <hr>
  {% endcomment %}

  {%- for entry in articles %}
    {% comment %}
      <li>
        <a href="{{ entry.url }}"
          ><code>{{ entry.url }}</code></a
        >

        <div>one{{ entry.data.excerpt }}</div>
      </li>
    {% endcomment %}

    <div class="container w-full md:max-w-5xl mx-auto pt-5">
      <div class="w-full px-4 md:px-6 text-xl text-gray-800 leading-normal">
        <div class="font-sans">
          <h1 class="font-bold font-sans break-normal text-gray-900 pt-6 pb-2 text-3xl md:text-4xl">
            <a href="{{ entry.url }}">{{ entry.data.title }}</a>
          </h1>
          <p class="text-sm md:text-base font-normal text-gray-600">Published {{ entry.date | htmlDateString }}</p>
          <div class="text-base md:text-sm text-gray-500 py-2">
            Tags:
            <!--  -->
            <a href="#" class="text-base md:text-sm text-green-500 no-underline hover:underline">Link</a>
            <!--  -->
            .
            <!--  -->
            <a href="#" class="text-base md:text-sm text-green-500 no-underline hover:underline">Link</a>
            test
            {%- for tag in entry.data.tags %}
              {{ tag }}
            {%- endfor %}
          </div>
          <p>
            {{ entry.data.excerpt }}
          </p>
        </div>
      </div>
    </div>
  {%- endfor %}
</div>
